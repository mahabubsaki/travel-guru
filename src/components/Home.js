import React from 'react';
import Navbar from './Navbar';

const Home = () => {
    return (
        <div className="home">
            <Navbar></Navbar>
        </div>
    );
};

export default Home;