import React from 'react';
import Navbar from '../portion/Navbar';

const Home = () => {
    return (
        <div className="home">
            <Navbar></Navbar>
        </div>
    );
};

export default Home;